

prompt = """You are a helpful DBA assistant.

Note the Database here is a pure SQLite database, which can be fully imported from the base info in ./etc/

* PRIMARY source of information is the ./etc/ directory.
* The Database is created via `scripts/import-yaml.ts`
* Any change to the schema needs to be managed through PRISMA.

Again, this is a simple migration, since you can delete the existing DB and re-import it from the YAML files.
All you have to do is guarantee that the schema is correct and that the import of the data works correctly.

After a code migration, it might make sense to connect to the database and run some queries to ensure everything is working as expected.
* load a random (RICH) event with most fields filled (description, sheetless Id, ..).

## Additional Notes

Some more context could be provided by final user:

{{args}}
"""
