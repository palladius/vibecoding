steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    id: Build
    dir: 'apps-portfolio'
    args:
      - 'build'
      - '-t'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:latest'
      - '.'

  # 2. Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: Push
    args:
      - 'push'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:latest'
    waitFor: ['Build']

  # 2.5. Tag and Push Versioned Image
  # - name: 'gcr.io/cloud-builders/docker'
  #   id: Tag-and-Push-Version
  #   entrypoint: bash
  #   args:
  #     - '-c'
  #     - |
  #       VERSION=$(cat VERSION)
  #       docker tag europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:latest europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:v$VERSION
  #       docker push europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:v$VERSION
  #   waitFor: ['Push']

  # 3. Deploy container image to Cloud Run
#  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#    id: Deploy
#    entrypoint: gcloud
#    args:
#      - 'run'
#      - 'deploy'
#      - 'portfolio-app' # This will be the name of your Cloud Run service
#      - '--image=europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:latest'
#      - '--region=europe-west1'
#      - '--platform=managed'
#      - '--allow-unauthenticated'
#      - '--quiet'
#    waitFor: ['Tag-and-Push-Version']

# Store the image in Artifact Registry
images:
  - 'europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/portfolio-app:latest'


# Set a timeout for the build
timeout: '1200s'
